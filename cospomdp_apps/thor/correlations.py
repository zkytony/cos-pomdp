# class SpatialCorrelationMatrix:
#     """
#     The spatial correlation between two classes depends on
#     the distribution of the euclidean distances between instances
#     of the classes. The expectation and variance of this
#     distribution tells how consistent the spatial separation
#     between the two objects is. Two classes are spatially correlated,
#     if, when compared with a reference, non-correlated pair,
#     the
#     """
#     def __init__(self, classes):
#         self.classes = classes

#     @staticmethod
#     def build(self, object_locations):
#         """
#         object_locations: list of (objclass, location) tuples.

#         Returns
#         """
